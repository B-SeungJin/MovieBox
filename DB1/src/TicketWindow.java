import javax.swing.*;
import java.awt.*;

class TicketWindow extends JFrame {
    public TicketWindow() {
        setTitle("Ticek에 대한 정보");
        setSize(300, 200);
        setLocationRelativeTo(null);
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        setVisible(true);
    }
}